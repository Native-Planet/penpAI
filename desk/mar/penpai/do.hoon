/-  *penpai
|_  d=do
++  grow
  |%
  ++  noun  d
  --
++  grab
  |%
  ++  noun  do
  ++  json
    =,  dejs:format
    %-  of
    :~  del+(ot name+so ~)
        new+(ot name+so prompt+so ~)
        post+(ot name+so who+(su (cold %user (jest 'user'))) what+so ~)
    ==
  --
++  grad  %noun
--
